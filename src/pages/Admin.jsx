// Admin page wrapper.
// Keeps routing clean and delegates UI to components.

import AdminDashboard from "../components/admin/AdminDashboard.jsx";

const Admin = () => {
  return <AdminDashboard />;
};

export default Admin;
